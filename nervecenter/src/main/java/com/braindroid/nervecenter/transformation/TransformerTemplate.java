package com.braindroid.nervecenter.transformation;

public class TransformerTemplate<T extends ViewModel, Z extends DataModel> {

    /*
    todo : define 'data model' and 'view model', so a template can have
     a set of Transformer's that operate on a 'data model'
     and produce a 'view model'
    */



}
